var omm_display = new display();
var omm_cssSelector_themaRow = ".omm_thema-row";

jQuery(document).ready(function(){
	
	var thematable = jQuery("#omm_thema-table");

	omm_display.init(thematable);
	
	
	
});
